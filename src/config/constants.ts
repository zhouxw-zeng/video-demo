import { lightTheme, darkTheme } from './theme';

// 可用的主题列表
export const themes = [
  { key: 'light', name: 'lightMode', config: lightTheme },
  { key: 'dark', name: 'darkMode', config: darkTheme },
];